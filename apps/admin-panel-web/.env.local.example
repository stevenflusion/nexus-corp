# Client-exposed vars required by the admin panel bundle.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_API_KEY=Pedirlallave

# Server-only vars used by Next.js Server Actions.
# These must NOT have the NEXT_PUBLIC_ prefix.
API_KEY=Pedirlallave
API_URL=http://localhost:4000/api
